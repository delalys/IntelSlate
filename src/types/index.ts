/**
 * IntelSlate Type Definitions
 *
 * This file serves as the central export point for all application types.
 * Import types from this barrel file: import { TypeName } from '@/types'
 */

// Re-export types from other type files as they are created
// Example: export * from './briefing';
// Example: export * from './source';
// Example: export * from './topic';

// Placeholder types - will be replaced as features are implemented
export type TPlaceholder = Record<string, unknown>;
